
import { TextStyle } from 'react-native';
import { colors } from './colors';

export const typography = {
  h1: {
    fontSize: 32,
    fontWeight: 'bold',
    color: colors.text,
  } as TextStyle,
  h2: {
    fontSize: 24,
    fontWeight: 'bold',
    color: colors.text,
  } as TextStyle,
  body: {
    fontSize: 16,
    color: colors.text,
  } as TextStyle,
};
